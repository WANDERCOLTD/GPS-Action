# GPS Action — API Contract

**Status:** Placeholder — derives from ERD.

This document will contain:

- Full tRPC router structure
- Every procedure, its input/output types
- Authentication and authorization requirements per endpoint
- Error codes per endpoint
- Rate limits per endpoint
- Versioning strategy

Once the ERD is complete, this document is mostly mechanical — generated
from the schema + service-layer design.

## Next session

After ERD. A dedicated session populates this from the primitive list and
the feature spec's journey sections.
