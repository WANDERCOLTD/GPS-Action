node_modules
.next
dist
build
coverage
.env
.env.local

# Auto-generated by scripts/trace.ts (BU-trace / D053).
# Drift check fails if prettier reformats this file; keep it as-rendered.
docs/architecture/traceability-matrix.md

# pnpm manages its own lockfile format; prettier's reformat diverges
# from pnpm's canonical output and would re-introduce drift on every
# `pnpm install`.
pnpm-lock.yaml
