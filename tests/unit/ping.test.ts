/**
 * Hello-world test — proves the test runner works.
 * Remove once real tests exist.
 */

import { describe, it, expect } from 'vitest';

describe('ping', () => {
  it('proves the test runner is alive', () => {
    expect(1 + 1).toBe(2);
  });
});
